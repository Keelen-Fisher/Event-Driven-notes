'use strict';

let eventPool = require('../eventPool');

module.exports = (payload) => {
  console.log('Pupils, Dialation update', payload);
}
// console.log('Brain: brightness has changed!');
